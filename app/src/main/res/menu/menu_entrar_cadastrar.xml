<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group  android:id="@+id/grupo_logado"
            android:visible="false">
        <item   android:id="@+id/menu_sair"
                android:title="Sair"></item>

        <item   android:id="@+id/menu_meusAnuncios"
                android:title="Meus anúncios"></item>
    </group>

    <group  android:id="@+id/grupo_deslogado"
            android:visible="false">
        <item   android:id="@+id/menu_autenticacao"
                android:title="Entrar / Cadastrar"
                app:showAsAction="always"></item>
    </group>

</menu>